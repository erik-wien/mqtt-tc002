import XCTest

/// `AppZustand` ist mit dieser Aufgabe nach `TC002Modell` gewandert (siehe
/// `Tests/TC002ModellTests/AppZustandTests.swift`) — `TC002App` bleibt bewusst
/// duenn und hat derzeit nichts Eigenes zu pruefen. Diese Datei haelt das Ziel
/// nicht leer: ein leeres Testziel-Verzeichnis meldet SwiftPM als Warnung.
final class PlatzhalterTests: XCTestCase {}
